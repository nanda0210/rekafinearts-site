# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Watermark script keeps originals here — don't commit (21 MB of dupes)
public/images/.watermark-backup/

# Local env (FTP / email creds — never commit)
.env
.env.local
# .env.production IS committed — non-secret backend URL only

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
